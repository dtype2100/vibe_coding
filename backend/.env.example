# Backend server port
PORT=5000

# PostgreSQL connection details
PG_HOST=localhost
PG_USER=your_postgres_user
PG_PASSWORD=your_postgres_password
PG_DATABASE=vibe_coding_guide
PG_PORT=5432

# Frontend URL (for CORS if needed, though current setup might be open)
# FRONTEND_URL=http://localhost:3000

# MongoDB URI (Kept for reference, but PostgreSQL is primary)
# MONGODB_URI=mongodb://localhost:27017/vibe-coding-guide
